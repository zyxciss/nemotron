[project]
name = "claude-code-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bitword-solver-rs",
    "jinja2>=3.1.6",
    "modal>=1.4.1",
    "pydantic>=2.12.1",
    "tinker>=0.16.1",
    "tinker-cookbook>=0.3.0",
    "torch>=2.11.0",
    "tqdm>=4.67.3",
    "transformers==4.57.6",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.13.2",
]

[tool.pytest.ini_options]
testpaths = [".claude"]

[tool.uv.workspace]
members = [
    "bitword_solver_rs",
]

[tool.uv.sources]
bitword-solver-rs = { workspace = true }
