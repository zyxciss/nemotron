"""
Claude Code Hook: Pre-WebFetch URL Validator.

Validates WebFetch URLs before execution.
"""


def validate_webfetch_url(url: str) -> list[str]:
    """Validate WebFetch URL before execution."""
    issues: list[str] = []

    return issues
